# Roadmap review — 2026-07-19 (content line closed → consumption + Notes era)

Ground truth from 4 read-only investigations (app repo / platform catalog+admin / Phase-B
foundations / open-threads register). Full agent reports in session workflow wf_5c399f2c.

## Where we actually are

- **Content supply: solved.** 95 published pieces, batch pipeline proven (Czerny 79 + Hanon 4
  in one night), upload guardrails live (rev 0000022). Admin pieces section stable.
- **App consumption: the bottleneck moved here.** The app browses with a hard-coded
  3-section flat list (mode×tier), decodes only 10 catalog fields, drops book/work/
  difficulty/tags/thumbnail/instrumentation at the struct boundary (CloudCatalog.swift:9-30),
  has no search/filter/sort/covers, piano-only soundfont, and no piece detail layer
  (row → straight into Performance). The platform catalog ALREADY serves nearly everything
  a rich Pieces tab needs (books[] with covers, works[] siblings, difficulty, facts,
  thumbnail_url, caps gates) — the gap is 90% app-side, 10% API polish.
- **Notes/Phase B: infra pre-provisioned, code greenfield.** lesson-audio + notes-assets
  containers and the notes-jobs SB queue already exist in bicep; users table has
  isTeacher/isStudent; ASR research locked (AssemblyAI universal-3-5-pro, GO thresholds met);
  LLM prompt spec prototyped. Zero schema/routes/worker code exists. 8 product decisions
  block the build (see decision memo).

## Tracks (two sessions, disjoint repos — multi-instance safe)

### Track 1 — App-side Pieces overhaul (app session, KaraOrcheeAMT)
The deferred "consolidated app batch" (founder 2026-07-11), conditions now met
(admin stable + real 95-corpus). Scope, grounded in the state map:
1. CloudPiece decoder expansion: book/work/difficulty/tags/thumbnail/instrumentation/facts.
2. Pieces tab IA redesign — mirror the finalized admin Library patterns, adapted to touch:
   Books cover grid as the primary shelf, works/sibling grouping, search (95 pieces),
   difficulty meter, sort, follow-ready visibility in rows.
3. Piece detail/preview layer (currently none): preview audio exists in catalog; a light
   detail sheet before Performance aids choice at 95-piece scale. Design call for founder.
4. Instrument gating + SF2 strategy (violin FluidR3 prog40 / guitar FreePats prog0 —
   mirror SOUNDFONTS memory) + flip caps=instruments. Unlocks violin/guitar corpus.
5. audio_map seek remnants + TestFlight build + founder/colleague repeat walkthrough.
Order: design doc → founder walkthrough → implementation waves (admin-2.0 playbook).

### Track 2 — This session (platform repo): Notes decision memo → Phase-D → Notes backend
1. **Notes decision memo first** (hours, unblocks everything): 8 founder decisions —
   ASR vendor ratification, LLM model (sonnet-5 vs deepseek ~30×), teacher review gate,
   relationship/invite semantics + who-pays, grounding scope v1 (absolute-only vs
   deixis-via-app-events), non-catalog pieces, consent/retention (lesson-audio auto-deletes
   raw at 90d — bicep lifecycle), identity unification (legacy pg vs platform CIAM).
2. **Phase-D (D.C./D.S.) implementation** while founder decides: kickoff pack ready
   (KaraOrcheeSibeliusPlugin/phase_d_kickoff.md), 4 real Czerny D.C. pieces waiting,
   dual-source expander design (music21 expandRepeats × sidecar BarPlaybackOrderString,
   reject-on-divergence), sidecar plugin ~half-day, classifier unlock + 3-way gate +
   launch-corpus regression. Also triggers sending the 3 jump-variant test files to colleague.
3. **Notes backend build** after decisions: schema (lessons/recordings/notes/jobs/
   relationships/invites) → SAS-mint direct-to-blob upload (multer path unusable for 60-min
   audio) → notes worker (clone pieces worker pattern; ASR→LLM→notes-assets+PG; DLQ+ops
   wiring) → review/retrieval API → admin surfaces. App surfaces follow in Track 1's repo.

### Track 3 — Colleague content ops (non-blocking, register kept)
Dolet re-uploads (2 published v2 + 10 staged), difficulty review of 79 Czerny in Review
queue, Czerny tail-numbering ×6 + repeat-source ×7 (or engine-rule rescue — founder call),
Hanon 43, No.41 duplicate ruling, Innocence reopen→publish.

### Track 4 — FOLLOW line (eval-gated, after Track 1)
follow_ready flips need: AMT-noise arm + golden scratch-regen byte-diff (g1 inputs / VM) +
Swift reset/reanchor + FOLLOW-tap. Value lands with repeat pieces in app.

### Track 5 — Prod readiness (before public beta)
docs/prod-checklist.md (8 items, none checked): prod RG/ACA workload profiles, logging
json-columns + 90d retention, identity redirect URIs, PG backup 35d + geo call, DLQ+cost
alerts, deploy wiring + CI gate, **licensed-piece exposure fix (hard beta blocker)**,
worker single-replica law. Plus GitHub remote (single-point risk, founder action).

## Verdict on the founder's proposed sequence

"App pieces → Notes backend → Notes app → teacher/friending/recording" is directionally
right with two corrections: (1) don't serialize — App track and platform track run in
parallel in separate sessions/repos; (2) teacher-student friending + recording upload are
PART of Notes backend v1 (the notes data model hangs off the relationship), not a later
phase. Phase-D slots into this session now because its kickoff pack is ready and the
founder-decision latency on Notes is free time.
