verovio>=6.2,<6.3
playwright>=1.61,<2
pretty_midi>=0.2.10
mido>=1.3
azure-servicebus>=7.12
azure-storage-blob>=12.27
psycopg[binary]>=3.2
librosa>=0.10
soundfile>=0.12
# thumbnail webp encode; already in the image transitively (matplotlib), stated
# here because pipeline/thumbnail.py imports it directly
pillow>=10
numpy<3
synctoolbox>=1.3
# note-evidence engine (audio gate): transcription model runs on CPU torch.
# torch itself is installed from the pytorch CPU index in the Dockerfile/CI (the
# default PyPI wheel drags CUDA); it is deliberately absent from the lock.
piano-transcription-inference>=0.0.6
